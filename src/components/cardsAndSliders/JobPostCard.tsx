import React from "react";

export default function JobPostCard({
  id,
  isActivelyHiring,
  title,
  companyName,
  isPartTime,
  isFullTime,
  isRemote,
  startDate,
  minSalary,
  maxSalary,
  jobPostDate,
  location,
  noOfOpening,
  minExperience,
  maxExperience,
}: any) {
  return <div className="rounded-lg bg-white"></div>;
}
